{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "concurrently -k -n \"vite,whatsapp\" -c \"cyan,green\" \"vite\" \"npm run start --prefix whatsapp-service\"",
        "whatsapp": "npm run start --prefix whatsapp-service"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.1",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    },
    "dependencies": {
        "@hotwired/turbo": "^8.0.23"
    }
}
